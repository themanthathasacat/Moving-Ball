import turtle
from random import random




def show_circle(t, color, pencolor, radius):
  t.pencolor(pencolor)
  t.fillcolor(color)
  t.begin_fill()
  t.circle(radius)
  t.end_fill()

if __name__ == '__main__':
  t = turtle.Turtle()
  t.speed(10000)
  t.hideturtle()


  def up():
    t.setheading(90)


def down():
  t.setheading(270)

def save():
    t.setheading(270)

def left():
  t.setheading(180)

def wave():
    t.setheading(90)

def right():
  t.setheading(0)

def an():
    t.setheading(0)

s = turtle.Screen()
s.bgcolor("Black")
s.tracer(0)
SCREEN_HEIGHT = 600
SCREEN_WIDTH = 800
step = 0.1
s.setup(SCREEN_HEIGHT, SCREEN_WIDTH)
s.listen()
s.onkey(up, 'Up')
s.onkey(down, 'Down')
s.onkey(left, 'Left')
s.onkey(right, 'Right')
s.onkey(wave,'w')
s.onkey(save, 's')
s.onkey(an, 'a')





























































































































































































































































































































































































































































































































































































x = 0
colors = ["white", "red", "orange", "yellow", "green", "blue", "purple"]
ballcolor = "white"
while True:
    t.clear()
    show_circle(t, ballcolor, ballcolor, 25)
    s.update()
    t.forward(step)
    x = x + step
    if x >= SCREEN_WIDTH/2:
      x = -SCREEN_WIDTH/2
      ballcolor = "red"
      t.goto(-SCREEN_WIDTH/2, 0)
      t.forward(step)






